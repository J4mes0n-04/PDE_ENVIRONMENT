# Паспорт модуля: `M-VAR · Переменные, связи и преобразования`

> **Scope-модуль:** `M-VAR` · **Владелец:** не определено в источниках · **Статус:** draft · **Веха / волна:** `В1 → В4`

<!-- ID-конвенции:
     модуль = карта v1.0 M-* · компонент = C-<MOD>-<n> · беклог = Б-<MOD>-<n>.
     Намеренно НЕ входит: карта зависимостей, открытые вопросы, кросс-роль-блокеры, каскадные последствия. -->

## Назначение

Единая модель «тегов» — значений состояния системы и оборудования, их связей с интерфейсом и преобразований. Фундамент, через который клиент и сценарии получают и меняют значения: без тегов и связей им нечего показывать и на что реагировать.

## Состав компонентов

| ID        | Компонент                       | Назначение (1 строка)                                                                                                                                                                                                                  | Приоритет | Scope `FM-*`                | Веха                                           | Статус       |
| --------- | ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | --------------------------- | ---------------------------------------------- | ------------ |
| `C-VAR-1` | Модель тегов                    | Единая сущность «тег» для драйверных, системных и пользовательских значений, включая persist-значения, переживающие перезапуск                                                                                                         | `P0`      | `FM-31, FM-32, FM-33`       | `В1` (срез) → `В2` (полно)                     | spec pending |
| `C-VAR-2` | Связи тегов                     | Автоматический проброс значений между тегами и элементами интерфейса с защитой от зацикливания → Автоматический проброс значений между тегами и свойствами элементов интерфейса, а также между тегами, с защитой от кольцевой передачи | `P0`      | `FM-34`                     | `В1` (срез) → `В2` (полно)                     | spec pending |
| `C-VAR-3` | Expression Evaluator            | Преобразования значений (диапазоны, типы, математика, логика) без программирования; расширенные выражения (строки, массивы, вложенные функции) — следующие версии                                                                      | `P0/P2`   | `FM-35` (P0), `FM-109` (P2) | `В2` (P0-часть) → `В4` (расширенные выражения) | spec pending |
| `C-VAR-4` | Отладка связей и преобразований | Диагностируемая ошибка преобразования (где, что пришло, что ожидалось) и просмотр связей тега — откуда приходит значение и куда уходит                                                                                                 | `P1`      | `FM-36, FM-37`              | `В3`                                           | spec pending |

## Беклог (post-MVP)

_Не используется на данном этапе — приоритеты компонентов см. в «Составе компонентов» выше (карта v1.0 не разделяет MVP/беклог, авторитет приоритетов — Product Scope & Technical Boundaries §1). Компоненты для беклога Eugeny дополнит/переместит сюда самостоятельно по мере работы._
