# Предлагаемые изменения

Каждый подкаталог — отдельный change и кандидат в один Outcome. Имя каталога английское, например `chg-001-device-status`.

Текущий intake:

- `chg-001-web-configurator` — веб-конфигуратор комплектов по `BRD-CFG-001` и `VIS-CFG-001`. Черновик Pack: `../../outcomes/OUT-CFG-001-web-configurator/`.
