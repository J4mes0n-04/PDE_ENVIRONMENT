# OpenSpec в проекте PDE

Каталог хранит разбор бизнес-требований **до** создания Outcome.

- `changes/chg-001-e1-main-page` — главная страница `Э-1` по Intent от 2026-09-22.
- `specs/` — текущее поведение после выпуска; до release не заполняется.
- Pack создаётся только в `outcomes/` после подтверждения change.

Не запускайте реализацию и `openspec archive` из этого каталога.
