# Эталонный проект OpenSpec intake

Фикстура платформы, не рабочий Outcome. На ней проверяются `validate-openspec-change.ps1` и `map-openspec-to-pack.ps1`.

Цепочка: `inputs/brd.md` → `openspec/changes/chg-001-device-status/` → `outcomes/OUT-FIX-001-device-status/`.
